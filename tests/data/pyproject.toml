[project]
name = "temporary-package"
version = "0.1.0"

requires-python = ">=3.14"
dependencies = [
    "astropy>=4",
    "plasmapy>=0.3.1",
    "numpy>=1.22.4",
]

[build-system]
requires = ["uv_build>=0.9.7,<0.10.0"]
build-backend = "uv_build"

# "cabinet>=2023.4.30.1,!=2024.7.17.1,!=1!2.1.1"
# "requests>=2.19.1"