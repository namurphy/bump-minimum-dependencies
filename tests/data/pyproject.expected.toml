[project]
name = "temporary-package"
version = "0.1.0"

requires-python = ">=3.14"
dependencies = [
    "astropy>=6",
    "plasmapy>=2024.2",
    "numpy>=2",
]

[build-system]
requires = ["uv_build>=0.9.7,<0.10.0"]
build-backend = "uv_build"
